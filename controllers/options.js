const Options = require('../models/options');
module.exports.delete = function (request, response) {
    return response.json(200);
}

module.exports.add_vote = function (request, response) {
    return response.json(200);
}